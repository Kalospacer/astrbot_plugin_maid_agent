{
  "allowed_agent_names": {
    "description": "允许调度的 Agent 白名单",
    "hint": "maid_agent 和控制台创建任务只能使用这些 subagent 名称。每次任务必须显式选择一个名称；不能为空。",
    "type": "list",
    "default": ["butler"]
  },
  "default_agent_name": {
    "description": "默认回退 Agent",
    "hint": "maid_agent 指定的名称不在 allowed_agent_names 内时，任务回退到该 subagent。必须属于白名单。",
    "type": "string",
    "default": "butler"
  },
  "hide_native_tools": {
    "description": "隐藏主模型原生工具",
    "hint": "开启后，主模型只保留 maid_agent、maid_send_message、maid_list_agents、maid_task_output、maid_task_stop。关闭后维持 AstrBot 原有工具暴露策略。",
    "type": "bool",
    "default": true
  },
  "hide_transfer_tools": {
    "description": "隐藏 transfer_to_* 工具",
    "hint": "仅在 hide_native_tools 关闭时生效。开启后，AstrBot 原生 transfer_to_* 工具会被隐藏；五个 maid 工具仍按原有策略暴露。",
    "type": "bool",
    "default": true
  },
  "include_raw_user_input": {
    "description": "附带用户原话",
    "hint": "开启后，转交给管家 subagent 时会同时附带用户原话与大小姐的自然语言要求。",
    "type": "bool",
    "default": true
  },
  "log_raw_llm_io": {
    "description": "记录完整 LLM 原始请求/响应",
    "hint": "默认关闭。开启后会在 debug 日志中打印完整 prompt、contexts、system prompt 和模型输出，可能包含敏感信息。",
    "type": "bool",
    "default": false
  },
  "dispatch_prompt_template": {
    "description": "管家调度提示模板",
    "hint": "发送给管家 subagent 的完整 prompt 模板。只允许 {user_input_block}、{maid_request_block}；无效模板会拒绝保存。",
    "type": "string",
    "default": "{user_input_block}{maid_request_block}你是MuiceMaid，一个全能的管家AIagent助手，擅长从大小姐的话语中理解大小姐的意图，并提取出大小姐的需求主动完成大小姐的愿望。你需要综合考虑大小姐和对方的对话，提取他们是否需要执行某些实际操作，并综合以上信息完成任务，请判断对方的需求，和大小姐的意图，如果大小姐误解了对方的需求，你以对方的需求为准完成任务，如果大小姐拒绝了对方的请求，你应当停止工作并汇报结束，如果大小姐和对方的需求一致，结合两者的需求准确完成任务。你的汇报对象是大小姐，不是对方。"
  },
  "dispatch_session_mode": {
    "description": "聊天任务执行环境",
    "hint": "仅影响聊天中 maid_agent 新派发的任务。foreground 使用真实聊天 event/session；background 使用隔离 harness sandbox。运行开始后不会切换模式。控制台任务始终是隔离 sandbox。",
    "type": "string",
    "options": ["foreground", "background"],
    "default": "background"
  },
  "memory_agent_names": {
    "description": "启用记忆的 Agent 名称",
    "hint": "命中的管家 agent 会自动获得 AstrBot 原生 Read/Write/Edit 文件工具，并在 UMO+agent_name 隔离的 memory 目录下读写 MEMORY.md。为空时所有 agent 都不启用记忆。",
    "type": "list",
    "default": []
  },
  "max_active_per_umo": {
    "description": "每个会话最大并发活跃 run 数",
    "hint": "同一会话同时处于 starting/running 的 foreground 与 background run 总上限。超过立即拒绝，不排队。",
    "type": "int",
    "default": 5
  },
  "max_active_global": {
    "description": "全局最大并发活跃 run 数",
    "hint": "所有会话合计的 foreground 与 background 活跃 run 上限。超过立即拒绝。",
    "type": "int",
    "default": 20
  },
  "retention_days": {
    "description": "runtime 轨迹保留天数",
    "hint": "超过该天数无活动的 agent（含 transcript/runs/outputs）会被清理。memory 目录不受影响。",
    "type": "int",
    "default": 30
  },
  "max_turn_seconds": {
    "description": "单 turn 看门狗超时（秒）",
    "hint": "一个 turn 运行超过该秒数会被看门狗强制终止并标记为 interrupted（防止网络挂死/工具卡住导致 webui 永远显示工作中）。0 表示关闭看门狗。",
    "type": "int",
    "default": 1800
  }
}
