{
  "default_agent_name": {
    "description": "默认管家 Agent 名称",
    "hint": "当 XML 中未显式指定 agent，或指定的 agent 不在白名单中时，回退到这个子 agent。",
    "type": "string",
    "default": "butler"
  },
  "allowed_agent_names": {
    "description": "允许调度的 Agent 白名单",
    "hint": "仅允许 XML 调度这些 subagent 名称。为空时仍会自动包含默认 agent。",
    "type": "list",
    "default": [
      "butler"
    ]
  },
  "call_tag_name": {
    "description": "XML 调度标签名",
    "hint": "主模型输出的 XML 标签名，例如 call_maid。修改后需与提示词和解析逻辑保持一致。",
    "type": "string",
    "default": "call_maid"
  },
  "done_tag_name": {
    "description": "Session 完成标签名",
    "hint": "大小姐声明当前管家任务结束时使用的独立 XML 标签名，例如 maid_session。",
    "type": "string",
    "default": "maid_session"
  },
  "include_raw_user_input": {
    "description": "附带用户原始输入",
    "hint": "开启后，转交给管家 subagent 时会同时附带用户原始输入与大小姐的自然语言要求。",
    "type": "bool",
    "default": true
  },
  "session_enabled": {
    "description": "启用管家 Session",
    "hint": "开启后，管家会在同一用户会话内持续复用自己的工作上下文，直到完成或超时。",
    "type": "bool",
    "default": true
  },
  "log_raw_llm_io": {
    "description": "记录完整 LLM 原始请求/响应",
    "hint": "默认关闭。开启后会在 debug 日志中打印完整 prompt、contexts、system prompt 和模型输出，可能包含敏感信息。",
    "type": "bool",
    "default": false
  },
  "session_timeout_minutes": {
    "description": "Session 超时时间（分钟）",
    "hint": "当前管家任务超过该时间未继续使用时，将自动失效并在下次重新创建。",
    "type": "int",
    "default": 20
  },
  "main_system_prompt_template": {
    "description": "主模型协议提示模板",
    "hint": "追加到主模型 system prompt 的模板。可用占位符：{call_tag_name}、{default_agent_name}、{done_tag_name}。",
    "type": "string",
    "default": "- 需要管家协助时，回复末尾附加：<{call_tag_name} agent=\"{default_agent_name}\">任务要求</{call_tag_name}>\n- 不需要管家则不附加此标签\n- 查询管家任务状态：<maid_control action=\"status\" />\n- 停止管家任务：<maid_control action=\"stop\" />\n- 补充或修正当前管家任务：<maid_control action=\"steer\">补充要求</maid_control>\n- 管家任务结束时附加：<{done_tag_name} status=\"done\" />，未结束不附加"
  },
  "dispatch_prompt_template": {
    "description": "管家调度提示模板",
    "hint": "发送给管家 subagent 的完整 prompt 模板。可用占位符：{user_input_block}、{maid_full_reply_block}、{maid_request_block}。",
    "type": "string",
    "default": "{user_input_block}{maid_full_reply_block}{maid_request_block}你是MuiceMaid，一个全能的管家AIagent助手，擅长从大小姐的话语中理解大小姐的意图，并提取出大小姐的需求主动完成大小姐的愿望。你需要综合考虑大小姐和用户的对话，提取他们是否需要执行某些实际操作，并综合以上信息完成任务，请判断用户的需求，和大小姐的意图，如果大小姐误解了用户的需求，你以用户的需求为准完成任务，如果大小姐拒绝了用户的请求，你应当停止工作并汇报结束，如果大小姐和用户的需求一致，结合两者的需求准确完成任务。你的汇报对象是大小姐，不是用户。"
  }
}
